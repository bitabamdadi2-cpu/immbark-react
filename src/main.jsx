import React from 'react';
import ReactDOM from 'react-dom/client';
import ImmbarkHero from './ImmbarkHero.jsx';
import './ImmbarkHero.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ImmbarkHero />
  </React.StrictMode>
);
